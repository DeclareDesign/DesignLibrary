% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designers.R
\name{two_arm_covariate_designer}
\alias{two_arm_covariate_designer}
\title{Create a two-arm design with a possibly prognostic covariate}
\usage{
two_arm_covariate_designer(
  N = 100,
  prob = 0.5,
  control_mean = 0,
  sd = 1,
  ate = 1,
  h = 0,
  treatment_mean = NULL,
  rho_WY = 0,
  rho_WZ = 0,
  args_to_fix = NULL
)
}
\arguments{
\item{N}{Sample size.}

\item{prob}{Probability of assignment to treatment.}

\item{control_mean}{Average outcome in control.}

\item{sd}{Standard deviation of the outcome shock.}

\item{ate}{Average treatment effect.}

\item{h}{Heterogeneity of the treatment effect by \code{W}.}

\item{treatment_mean}{Average outcome in treatment. If supplied, overrides
\code{ate} (\code{ate} becomes \code{treatment_mean - control_mean}).}

\item{rho_WY}{Correlation between \code{W} and the outcome shock (-1 to 1).}

\item{rho_WZ}{Correlation between \code{W} and the latent assignment variable
(-1 to 1).}

\item{args_to_fix}{Ignored. Present for compatibility with DesignLibrary 0.1.}
}
\value{
A design object.
}
\description{
Routes to \code{\link[=make_design]{make_design()}} with id \code{"two_arm_covariate"}:
\code{make_design("two_arm_covariate", N = N, rho_WY = rho_WY, ...)}.
}
\details{
The covariate \code{W} can predict the outcome (\code{rho_WY}), moderate the effect
(\code{h}), and predict assignment (\code{rho_WZ}). With \code{rho_WZ = 0} assignment is
random and adjustment buys precision; otherwise \code{W} confounds. Argument
names match DesignLibrary 0.1's \code{two_arm_covariate_designer}.
}
\examples{
\dontrun{
prognostic <- two_arm_covariate_designer(N = 40, ate = 0.2, rho_WY = 0.9, h = 0.5)
confounded <- two_arm_covariate_designer(N = 40, ate = 0, rho_WZ = 0.9, rho_WY = 0.9)
}
}
\seealso{
\code{\link[=make_design]{make_design()}}
}
