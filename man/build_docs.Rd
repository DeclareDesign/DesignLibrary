% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintain.R
\name{build_docs}
\alias{build_docs}
\title{Build the pkgdown site (Dropbox-safe)}
\usage{
build_docs(pkg = NULL, ...)
}
\arguments{
\item{pkg}{Package root. Default: \code{find_package_root()} via
\code{options(DesignLibrary.root=...)} or the current working directory.}

\item{...}{Passed to \code{\link[pkgdown:build_site]{pkgdown::build_site()}} (for example \code{devel = TRUE}).}
}
\value{
Invisibly, the path to \verb{docs/}.
}
\description{
On Windows Dropbox folders, \code{pkgdown::build_site()} / \code{build_article()} often
fail in \code{xml2::write_html()} with "Invalid argument" / "Error closing file"
when writing directly into \verb{docs/}. This helper builds into a local temp
directory, then copies the result into \verb{docs/}.
}
\examples{
\dontrun{
options(DesignLibrary.root = "C:/path/to/DesignLibrary")
build_docs()
}
}
