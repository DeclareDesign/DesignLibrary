% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designers.R
\name{factorial_designer}
\alias{factorial_designer}
\title{Create a 2^k factorial design}
\usage{
factorial_designer(
  N = 256,
  k = 3,
  outcome_means = rep(0, 2^k),
  sd = 1,
  outcome_sds = rep(sd, 2^k),
  assignment_probs = rep(0.5, k),
  outcome_name = "Y",
  treatment_names = NULL,
  args_to_fix = NULL
)
}
\arguments{
\item{N}{Sample size.}

\item{k}{Number of factors, an integer of at least 2.}

\item{outcome_means}{Mean outcome in each of the \code{2^k} treatment
combinations.}

\item{sd}{Standard deviation of every potential outcome, used when
\code{outcome_sds} is not supplied.}

\item{outcome_sds}{Standard deviation of the outcome in each of the \code{2^k}
treatment combinations.}

\item{assignment_probs}{Probability of assignment to each factor (length
\code{k}).}

\item{outcome_name}{Name of the outcome variable.}

\item{treatment_names}{Names of the \code{k} factors. \code{NULL} gives \code{T1}, ...,
\code{Tk}.}

\item{args_to_fix}{Ignored. Present for compatibility with DesignLibrary 0.1.}
}
\value{
A design object.
}
\description{
Routes to \code{\link[=make_design]{make_design()}} with id \code{"factorial"}:
\code{make_design("factorial", N = N, k = k, ...)}.
}
\details{
Each of \code{k} factors is assigned with its own probability, and each of the
\code{2^k} treatment combinations has its own normally distributed potential
outcome. The inquiries are the overall average, the average effect of each
factor, and every interaction, each averaged with equal weight over the
conditions of the other factors. The estimator regresses the outcome on the
full interaction of the treatments centered at 0.5, weighted by the inverse
probability of the assigned combination, so the intercept estimates the
overall average and each coefficient an average effect. Argument names
match DesignLibrary 0.1's \code{factorial_designer}.

\code{outcome_means} and \code{outcome_sds} follow the row order of
\code{expand.grid(rep(list(c(0, 1)), k))}, in which the first factor varies
fastest.
}
\examples{
\dontrun{
factorial_designer(k = 3, assignment_probs = c(1/2, 1/4, 1/8),
                   outcome_means = c(0, 0, 0, 0, 0, 0, 0, 4))
}
}
\seealso{
\code{\link[=make_design]{make_design()}}
}
