% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designers.R
\name{binary_iv_designer}
\alias{binary_iv_designer}
\title{Create a binary instrumental variables design}
\usage{
binary_iv_designer(
  N = 100,
  type_probs = c(1/3, 1/3, 1/3, 0),
  assignment_probs = c(0.5, 0.5, 0.5, 0.5),
  a_Y = 1,
  b_Y = 0,
  d_Y = 0,
  outcome_sd = 1,
  a = c(1, 0, 0, 0) * a_Y,
  b = rep(b_Y, 4),
  d = rep(d_Y, 4),
  args_to_fix = NULL
)
}
\arguments{
\item{N}{Sample size.}

\item{type_probs}{Shares of always-takers, never-takers, compliers, and
defiers.}

\item{assignment_probs}{Probability that Z = 1 for each type.}

\item{a_Y}{Outcome intercept for always-takers, used in the default \code{a}.}

\item{b_Y}{Effect of X on the outcome for every type, used in the default
\code{b}.}

\item{d_Y}{Direct effect of Z on the outcome for every type, used in the
default \code{d}.}

\item{outcome_sd}{Standard deviation of the outcome shock.}

\item{a, b, d}{Length-4 outcome intercepts, effects of X, and direct effects
of Z, by type.}

\item{args_to_fix}{Ignored. Present for compatibility with DesignLibrary 0.1.}
}
\value{
A design object.
}
\description{
Routes to \code{\link[=make_design]{make_design()}} with id \code{"binary_iv"}:
\code{make_design("binary_iv", N = N, type_probs = type_probs, ...)}.
}
\details{
Units are always-takers, never-takers, compliers, or defiers, in that order
in every length-4 argument. \code{a_Y}, \code{b_Y}, and \code{d_Y} fill the defaults of
\code{a}, \code{b}, and \code{d} and are not passed on. Argument names match DesignLibrary
0.1's \code{binary_iv_designer}. See also \code{make_design("encouragement")} and
\code{make_design("instrumental_variables")}.
}
\examples{
\dontrun{
binary_iv_designer(N = 200, b_Y = 0.5)
binary_iv_designer(type_probs = c(0.2, 0.2, 0.5, 0.1), b = c(0, 0, 1, -1))
}
}
\seealso{
\code{\link[=make_design]{make_design()}}
}
