% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/designers.R
\name{multi_arm_designer}
\alias{multi_arm_designer}
\title{Create a multi-arm design}
\usage{
multi_arm_designer(
  N = 90,
  m_arms = 3,
  outcome_means = rep(0, m_arms),
  sd_i = 1,
  outcome_sds = rep(0, m_arms),
  conditions = seq_len(m_arms),
  Y = NULL,
  args_to_fix = NULL
)
}
\arguments{
\item{N}{Sample size.}

\item{m_arms}{Number of arms. May vary. Must match \code{length(outcome_means)}
unless \code{outcome_means} is a scalar (then recycled).}

\item{outcome_means}{Average outcome in each arm. Default \code{rep(0, m_arms)}.}

\item{sd_i}{Standard deviation of the individual-level shock.}

\item{outcome_sds}{Extra standard deviation in each arm. Default
\code{rep(0, m_arms)}.}

\item{conditions}{Assignment conditions, length \code{m_arms}. Default
\code{seq_len(m_arms)}.}

\item{Y}{Optional outcome function of \code{Z}, \code{u}, and \code{outcome_sds}. The
library default is used when \code{Y} is \code{NULL}.}

\item{args_to_fix}{Ignored. Present for DesignLibrary compatibility.}
}
\value{
A design object.
}
\description{
Routes to \code{\link[=make_design]{make_design()}} with id \code{"multiarm_trial"}:
\code{make_design("multiarm_trial", N = N, m_arms = m_arms, ...)}.
Defaults for \code{outcome_means}, \code{outcome_sds}, and \code{conditions} are
evaluated after \code{m_arms}, so \code{multi_arm_designer(m_arms = 4)} expands
those vectors before calling \code{make_design()}. \code{make_design("multiarm_trial", m_arms = 4)} does the same inside the design: constant vectors are repeated
and conditions \verb{1..k} become \verb{1..m_arms}. Any other wrong-length vector
errors.
Inquiries are \code{mean(Y(k) - Y(1))}; estimation is one \code{lm_robust}.
Library-file knobs match these formals.
}
\examples{
\dontrun{
make_design("multiarm_trial", N = 90)
multi_arm_designer(m_arms = 4)
multi_arm_designer(m_arms = 4, outcome_means = c(0, 0.5, 1, 2))
}
}
\seealso{
\code{\link[=make_design]{make_design()}}
}
